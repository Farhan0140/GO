- 2 Phases:
    1. compilation phase
    2. execution phase 

```
go build main.go
```
```
./main
```

`go run main.go => compile it => main => ./main`

`build main.go => compile it => main`

`./main`

---

![image alt]()